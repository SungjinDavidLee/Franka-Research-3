# 00. Baseline — 파인튜닝 이전 평가

`openvla-7b-finetuned-libero-spatial` 체크포인트를 그대로 사용해
실물 Franka에서 Task6를 평가한 기록.

---

## 설정

| 항목 | 값 |
|---|---|
| 입력 | D405 third-person RGB 1장 + 언어 명령 |
| 전처리 | `center_crop_90` → 256×256 |
| 액션 | 7D (dx, dy, dz, rx, ry, rz, gripper) |
| 제어 | Cartesian velocity streaming |
| unnorm_key | `libero_spatial` |

명령문은 LIBERO 원문을 그대로 사용했다.

```
Pick up the black bowl next to the cookie box and place it on the plate.
```

방향 힌트나 좌표는 넣지 않았다.

---

## 씬 구성

LIBERO 시뮬에서 settle 후 world 좌표를 뽑아 실제 책상 배치의 기준으로 삼았다.

| 물체 | x | y | 쿠키박스와의 거리 |
|---|---|---|---|
| akita_black_bowl_1 (타깃) | 0.129 | -0.064 | 0.107 |
| akita_black_bowl_2 (비타깃) | -0.260 | -0.134 | 0.362 |
| cookies_1 | 0.055 | 0.032 | — |
| plate_1 | 0.061 | 0.187 | — |
| ramekin_1 | -0.212 | 0.201 | — |

타깃이 쿠키박스에서 11cm, 비타깃이 36cm로 3배 이상 차이가 나
"next to the cookie box"라는 관계가 모호하지 않다.

이 좌표는 물리 배치와 성공 판정에만 사용했고 실행 코드에서 참조하지 않았다.

---

## 결과: 실패

정지 씬 20 샘플 (씬 배치 조정 후):

| 항목 | 값 |
|---|---|
| Y 부호 | 20/20 양수 (평균 +0.240) |
| xyz 평균 | [0.380, 0.240, -0.611] |
| gripper | OPEN 20 / CLOSE 0 |

live rollout에서 관찰된 증상:

- **X 포화** — 토큰 상한 `+0.934`에 20~44 스텝 붙어 있음. 목표에 접근해도 감속 없음
- **Y 편향** — 씬과 무관하게 거의 항상 양수. 로봇이 계속 한쪽으로 이동
- **액션 진동** — 정지 씬에서도 같은 관측에 z가 +0.31 / -0.54로 흔들림
- **조기 CLOSE** — 초기 크롭 조건에서 그리퍼 예측이 100% CLOSE

---

## 가설 검증

### [기각] 이미지 좌우 반전

4-way orientation test (RAW / ROT180 / HFLIP / VFLIP):

| 입력 | X | Y | Z | grip |
|---|---|---|---|---|
| 실제 RAW | +0.78 | +0.38 | -0.12 | OPEN |
| 실제 HFLIP | +0.66 | +0.34 | +0.77 | CLOSE |

좌우를 뒤집었는데 **Y 부호가 바뀌지 않았다.**
이미지 반전이 원인이라면 Y가 음수로 뒤집혀야 한다.

ROT180과 VFLIP은 액션이 거의 0으로 소멸했다.
상하 반전은 학습 분포 밖이라 모델이 판단을 못 하는 것이고,
현재 상하 방향은 올바르다는 뜻이다.

### [기각] 어댑터 Y 부호 오류

모델 Y가 씬과 무관하게 양수이므로, 부호를 뒤집어도
"항상 반대쪽"이 될 뿐 씬 반응성은 생기지 않는다.

### [확인] 모델·프롬프트·checkpoint는 정상

LIBERO 시뮬 렌더 이미지를 입력했을 때:

| 입력 | X | Y | Z |
|---|---|---|---|
| 시뮬 RAW | +0.86 | -0.019 | -0.003 |
| 시뮬 HFLIP | -0.010 | -0.001 | -0.003 |

시뮬 이미지에서는 Y가 0에 가깝고, HFLIP을 걸면 액션이 소멸한다.
**모델은 좌우를 읽을 수 있다.** 문제는 실제 관측 쪽이다.

### [버그] 전처리 크롭

`center_crop_90`이 크롭 후 **원본 크기(848×480)로 되돌리고** 있었다.

```python
def center_crop_90(image):
    w, h = image.size
    scale = 0.9 ** 0.5
    ...
    image = image.resize((w, h), Image.Resampling.BILINEAR)  # 848×480 복원
    return image
```

LIBERO 원본 평가 코드가 정사각형 256×256 입력을 전제로 짜여 있어
`(w, h)`로 되돌려도 문제가 없었던 것이다.
D405는 848×480 직사각형이라 종횡비가 16:9로 유지됐고,
좌우로 넓어 벽·냉장고·서랍장이 프레임에 들어왔다.

중앙 정사각형 크롭 후 256×256으로 수정했다.

수정 후 그리퍼 예측이 100% CLOSE에서 OPEN/CLOSE 혼재로 개선됐다.
좌우 문제는 그대로였다.

### [실패] 크롭 위치 스윕

정사각형 창을 -120 ~ +120 px 이동해 7개 후보를 비교:

| 후보 | X | Y | Z | grip |
|---|---|---|---|---|
| shift_-120 | -0.326 | +0.137 | -0.003 | CLOSE |
| shift_-080 | +0.353 | -0.001 | -0.641 | OPEN |
| shift_-040 | +0.578 | +0.300 | +0.540 | CLOSE |
| shift_+000 | +0.776 | -0.001 | +0.298 | CLOSE |
| shift_+040 | +0.710 | +0.113 | -0.010 | CLOSE |
| shift_+080 | +0.578 | +0.403 | -0.003 | OPEN |
| shift_+120 | +0.400 | -0.001 | -0.626 | CLOSE |

**40px 이동만으로 전진/후진, 상승/하강이 뒤집힌다.**
같은 씬, 같은 물체, 같은 명령인데 프레이밍만 바꿨는데도 그렇다.

Y는 7개 모두 0 또는 양수. 음수가 하나도 없다.
프레이밍 과민성만 확인했고 해결하지 못했다.

### [실패] 배경 제거

`preprocess_real_to_simlike_v3`로 테이블 밖 영역을 회색(150,150,150) 처리.
동일 크롭 조건으로 비교:

| 조건 | X | Y | Z |
|---|---|---|---|
| 원본 | +0.782 | +0.384 | -0.120 |
| 배경 제거 | +0.928 | +0.354 | -0.010 |

Y 변화가 거의 없다. 정사각형 크롭 후에는 제거된 영역(오른쪽 냉장고, 위쪽 벽)이
대부분 잘려나가 실질적 차이가 없었다.

### [부분 성공] 카메라 재배치

삼각대를 10~15cm 올리고 아래로 기울인 뒤 좌측으로 5cm 이동:

| 항목 | 이동 전 | 이동 후 |
|---|---|---|
| Y 음수 | 0/20 | 4/20 |
| Y 평균 | +0.476 | +0.144 |
| Z 평균 | -0.010 | -0.393 |
| OPEN/CLOSE | 9/11 | 13/7 |

Y 음수가 처음 등장했고 Z 하강이 뚜렷해졌다. 초반 접근이 개선됐다.

그러나 live 궤적에서는 52 스텝 중 51개가 Y 양수였고,
스텝 32부터 X가 다시 `+0.934` 상한에 포화됐다.

### [실패] Scene graph 언어 보강 (augmented condition)

GroundingDINO + SAM2로 물체를 검출하고
`next_to(black_bowl, cookie_box)` 관계를 풀어 타깃을 지정한 뒤,
그 결과를 자연어로 프롬프트에 덧붙였다.

```
Pick up the black bowl next to the cookie box and place it on the plate.
The target bowl is to the left of the cookie box.
```

인식 자체는 정확했다. 타깃 픽셀 `[354.8, 330.3]`이 항상 올바른 그릇을 가리켰고
시각화로 확인했다.

| 항목 | baseline | + scene graph |
|---|---|---|
| Y near_zero | 0/20 | 20/20 |
| Y 평균 | +0.144 | -0.001 |
| Z 평균 | -0.393 | -0.655 |
| OPEN/CLOSE | 13/7 | 20/0 |

**좌우 문제는 해소됐으나 거리 판단은 그대로였다.**
live 궤적에서 X가 전 구간 양수로 감속 없이 진행해 타깃을 통과했다.

프롬프트를 더 정밀하게 만드는 시도는 역효과였다:

| 프롬프트 | Y | Z | OPEN/CLOSE |
|---|---|---|---|
| v1: 원문 + "to the left of ..." | -0.001 | -0.655 | 20/0 |
| v2: "directly ... immediately adjacent" | +0.236 | -0.083 | 20/0 |
| v3: 원문을 "to the left of"로 교체 | +0.470 | +0.181 | 2/18 |

v3에서 원래 instruction의 `next to the cookie box`를 통째로 교체했더니
그리퍼가 18/20 CLOSE로 뒤집혔다.
**LIBERO 학습 문장을 보존하는 것이 중요하다**는 점이 확인됐다.

### [버그] 어댑터 Y flip 누락

TCP 로그 분석에서 발견했다.

```
액션 Y 양수 구간에서 tcp_y 증가 (총 dy = +0.0705 m)
문서 규약: Y_robot = -Y_OpenVLA
```

컨트롤러 소스를 대조하니:

```cpp
openvla_cartesian_pose_controller.cpp:74        -action[1]   // 반전 있음
openvla_smooth_6dof_cartesian_pose_controller   -action[1]   // 반전 있음
openvla_translation_only_diagnostic_controller   action[1]   // 없음 (진단용)
openvla_cartesian_velocity_controller.cpp:66     action[1]   // 누락 ★
```

end2end(pose) → velocity streaming 전환 시 누락된 것으로 보인다.

컨트롤러에 반전을 넣었더니 `return_to_trial_start`가 깨졌다.
그 스크립트는 이미 로봇 좌표계로 계산된 명령을 보내는데,
컨트롤러가 뒤집으면 목표에서 멀어지기 때문이다.

**OpenVLA 액션을 다루는 rollout 층에서 반전**하도록 수정했다.

수정 후 로그에서 확인:

```
raw_xyz=[+0.2002, -0.0003, ...] -> smooth_xyz=[+0.2002, +0.0003, ...]
```

---

## 시뮬 대조 실험

동일 checkpoint로 LIBERO 시뮬에서 Task6 수행 → **성공**.

```
같은 모델 / 같은 프롬프트 / 같은 unnorm_key
  시뮬 관측 → 성공
  실사 관측 → 실패
```

모델 능력의 한계가 아니라 sim-to-real 도메인 갭이 원인임이 확정됐다.
이로써 앞선 모든 실패를 일관되게 설명할 수 있다.

---

## 결론

관측 후처리(크롭 조정, 배경 제거)와 카메라 재배치로는 부분 개선에 그쳤다.
scene graph 언어 보강은 좌우 문제를 해결했으나 거리 판단은 개선하지 못했다.

VLA 모델의 실패가 단일 원인이 아님이 드러났다.

| 실패 층위 | 성격 | 언어 보강으로 해결? |
|---|---|---|
| 좌우 (Y) | semantic grounding | 가능 |
| 전후·상하 (X, Z) | 시각적 거리 추정 | 불가능 |

거리 정보를 추가로 주입하면 사람이 설계한 서보 제어에 가까워져
OpenVLA 평가로서의 타당성을 잃는다고 판단해,
실사 데이터 파인튜닝으로 방향을 전환했다.
