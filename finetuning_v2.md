# Fine-tuning v2 — 액션을 실측 TCP delta로 재계산

v1의 액션 이산화 실패를 해결하기 위해 **동일한 시연 데이터에서
액션 기록 방식만 바꿔** 재학습했다.

모델이 관측에 반응하기 시작했고 접근·하강을 학습했으나,
**grasp는 여전히 실패**했다.

---

## 변경점

### 1. 액션 역산

v1은 사람이 보낸 **명령값**을 기록했다.

```python
action[0:3] = self.cmd          # -0.3, 0, +0.3 세 값뿐
```

v2는 **실제 로봇이 움직인 양**에서 역산한다.

```python
def tcp_to_action(tcp, t):
    """컨트롤러 규약과 대칭:
       velocity_mps = action[0:3] × k_position_scale_(0.05)
       따라서 action = (dTCP/dt) / 0.05
    """
    dt = np.diff(t)
    act = np.diff(tcp, axis=0) / dt[:, None] / 0.05
    act[:, 1] *= -1.0          # 로봇 → OpenVLA 좌표계
    return np.clip(act, -1.0, 1.0)
```

로봇은 명령대로 정확히 움직이지 않는다. 가속·감속이 있고 지연도 있다.
그 실제 응답을 학습해야 모델이 낸 액션이 의도한 만큼 움직인다.

### 2. no-op 필터링

액션이 거의 0인 프레임을 제거했다.

```python
NOOP_EPS = 0.02
keep = [i for i in range(len(frames)) if np.abs(xyz[i]).max() > NOOP_EPS]
```

LIBERO 데이터셋도 `libero_spatial_no_noops` 버전을 쓰는 이유가 이것이다.

### 3. 시도했으나 채택하지 않은 방법

**이동평균 평활**을 먼저 시도했다.

| 방식 | x 고유값 | y | z |
|---|---|---|---|
| 원본 (명령값) | 2 | 2 | 3 |
| 이동평균 (window=5) | 6 | 6 | 11 |
| **TCP delta 역산** | **26** | **27** | **42** |

평활은 전환 구간만 부드럽게 만들 뿐,
키를 누른 채 수십 프레임이 지나는 구간은 그대로였다.
TCP delta 역산이 훨씬 나아 그쪽을 택했다.

---

## RLDS v2

`task6/rlds/task6_franka_v2/`

| 항목 | v1 | v2 |
|---|---|---|
| 에피소드 | 20 | 20 (동일) |
| 프레임 | 10,821 | 9,003 |
| 용량 | 842 MiB | 700 MiB |
| x 고유값 | 2 | **140** |
| y 고유값 | 2 | **142** |
| z 고유값 | 3 | **208** |
| 액션 범위 | ±0.3 (3단계) | ±0.2 (연속) |

X축 분포:

```
-0.208 근처:   440
 0    근처: 6,062
+0.167 근처: 2,413
```

여전히 세 덩어리에 몰려 있다. 키보드 jog의 흔적이다.
가속·감속 구간이 짧아 중간값이 적고, 한 축을 움직일 때
나머지 두 축은 0이라 0 근처가 67%다.

완벽하진 않지만 256단계 중 3개만 쓰던 v1보다는 크게 나아졌다.

---

## 학습 설정

```bash
--dataset_name task6_franka_v2 \
--batch_size 8 \
--max_steps 2000 \
--save_steps 1000 \
--learning_rate 5e-4 \
--use_lora True --lora_rank 32
```

v1의 5000 스텝은 과했다. 체크포인트 저장에 9분씩 걸려
2000 스텝, 저장 2회로 줄였다. 총 35분.

---

## 결과

### 1. 모델이 관측에 반응한다

학습 데이터 이미지 3장을 넣어 확인:

| 이미지 | 토큰 | 예측 | 정답 |
|---|---|---|---|
| ep_001 / 050 | `[31745, 31872, ...]` | X +0.200 | X +0.190 |
| ep_001 / 200 | `[31872, 31872, 31999, ...]` | Z -0.201 | Z -0.201 |
| ep_001 / 400 | `[31872, 31999, 31872, ...]` | Y -0.200 | Y -0.201 |

**서로 다른 이미지에 서로 다른 액션**이 나왔다.
v1에서 `31872` 하나만 나오던 것과 완전히 다르다.

3장 중 2장은 정답과 거의 일치했다.

### 2. 실물에서 타깃 접근 성공

정지 씬 20 샘플:

| 항목 | baseline | v1 | v2 |
|---|---|---|---|
| Y | +0.240 (20/20 양수) | 0.000 | **-0.200 (20/20 음수)** |
| gripper | OPEN 20/0 | OPEN | OPEN 20/0 |

live 궤적 (arm-only, 100 스텝):

```
step 1~60:   X +0.200 전진, z 유지
step 61~97:  X 고정, Z -0.199 하강
step 98~100: (스텝 제한으로 종료)
```

**접근 → 하강 전환이 명확히 일어났다.**
baseline에서 X가 `+0.934` 상한에 포화되던 것과 달리
학습한 속도(0.2)를 안정적으로 유지한다.

물체 위치를 바꿔도 타깃을 향해 갔다.
관측을 보고 판단한다는 뜻이다.

### 3. grasp 실패

전체 rollout에서 하강 후 그리퍼가 닫히지 않았다.

Z 하한 안전장치(`Z_MIN = 0.070`)를 넣었더니 교착 상태가 됐다.

```
[WARN] Z LIMIT | tcp_z=0.0661 < 0.07 -> descent blocked
raw_xyz=[+0.0002, -0.0003, -0.2007] -> smooth_xyz=[..., +0.0000]
gripper_raw=0.9961 -> OPEN
```

19 스텝 동안 하강 명령이 계속 나오는데 차단되고,
그리퍼는 끝까지 OPEN이었다.

---

## 원인 분석

### 학습 데이터로 직접 검증

정답이 CLOSE인 프레임을 모델에 넣어봤다.

| 에피소드 | 프레임 | 정답 | 예측 |
|---|---|---|---|
| ep_001 | 306 | OPEN | OPEN (0.996) |
| ep_001 | **311** | **CLOSE** | **OPEN (0.996)** |
| ep_001 | 314 | CLOSE | OPEN (0.996) |
| ep_010 | 347 | CLOSE | OPEN (0.996) |
| ep_020 | 208 | CLOSE | OPEN (0.996) |

**모델이 grasp 자체를 못 배웠다.**
실시간 관측 문제가 아니라 학습 문제다.

### 원인 1: grasp 대기 구간

수집 시 하강이 끝난 뒤 **약 3초간 정지한 채로** 그리퍼를 닫았다.

```
frame  -30: z=0.1153  OPEN
frame  -15: z=0.0915  OPEN   ← 하강 완료
frame   -5: z=0.0915  OPEN
frame   +0: z=0.0915  CLOSE  ← 여기서 닫음
```

-15부터 z가 고정이다. 15프레임 동안 **화면이 완전히 동일한데
정답만 OPEN에서 CLOSE로 바뀐다.**

같은 입력에 다른 정답이 있으니 모델이 배울 수 없다.

20 에피소드 전체 평균 8.7 프레임, 최대 18 프레임이었다.

### 원인 2: 로봇 팔이 타깃을 가림

grasp 전후 이미지를 확인하니, **타깃 검은 그릇이
로봇 팔에 완전히 가려져 있었다.**

모델이 볼 수 있는 건 로봇 팔, 쿠키박스 일부, 접시뿐이다.
그리퍼가 그릇 위에 도달했는지 판단할 근거가 화면에 없다.

third-person 시점의 구조적 한계다.

### 원인 3: 데이터 불균형

9,003 프레임 중 CLOSE 전환점은 20개뿐(0.2%)이다.
모델이 "항상 OPEN"이라고 해도 손실이 거의 늘지 않는다.

---

## 부수적 발견

### Z 하한 안전장치

학습 데이터의 grasp 시 z 분포:

```
범위 0.0727 ~ 0.0915  평균 0.0799  표준편차 0.0053
```

20개가 2cm 안에 들어온다.
안전장치로 `Z_MIN = 0.070`을 두어 그 아래로는 하강을 차단했다.

목표 좌표를 지정하는 것이 아니라 물리적 파손을 막는 상한 제약이다.
다른 축과 그리퍼는 그대로 두어 모델이 판단할 여지를 남겼다.

### 프롬프트 마침표

writer가 `INSTRUCTION.lower()`를 써서 `...plate.?`가 되고 있었다.
`.rstrip('.')`으로 수정했다. 결과에 영향은 없었다.

---

## 다음 단계

grasp 대기 구간이 명확한 원인으로 확인됐다.
수집 시 **하강 완료 즉시 그리퍼를 닫도록** 방식을 바꾸고 재수집한다.

→ [finetuning_v3.md](finetuning_v3.md)
